
<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>

    </aside>
    <div class="col-md-8">
      <h3>ユーザー一覧</h3>
      <%= render 'index' %>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>twitters</h1>

    <%= link_to "今すぐアカウントを作ろう!", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
